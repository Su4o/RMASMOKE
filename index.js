import "flowbite";

var paragraphs = document.querySelectorAll("p");

// Loop through each <p> element and add the 'kmu' class
paragraphs.forEach(function (paragraph) {
  paragraph.classList.add("kmu");
});
